# BankManagementApp
This project models a simplified banking system where users can manage
accounts, track balances, and process transactions. The focus is on
backend logic, validation, and state management rather than UI design.

## How to Run
Build artifacts are excluded from version control.
Run or debug the application using the provided VS Code tasks
or execute the main program files from the `src` directory.
